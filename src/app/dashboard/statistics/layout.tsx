import { api, HydrateClient } from "@/trpc/server";
import { type Metadata } from "next";

interface DashboardStatisticLayoutProps {
  children: React.ReactNode;
}

export const metadata: Metadata = {
  title: "Statistics | My Portfolio",
  description: "Generated by create-t3-app",
  icons: [{ rel: "icon", url: "/favicon.ico" }],
};
export default function DashboardStatisticLayout({
  children,
}: DashboardStatisticLayoutProps) {
  api.project.all.prefetch();

  return (
    <section>
      <div className="flex items-center">
        <h1 className="text-lg font-semibold md:text-2xl">Statistics</h1>
      </div>
      <div className="flex flex-col flex-wrap gap-4 p-4 lg:gap-6 lg:p-6">
        <HydrateClient>{children}</HydrateClient>
      </div>
    </section>
  );
}
