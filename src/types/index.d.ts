export type Skills = {
  name: string;
  icon: JSX.Element;
};

export type SocialMedia = {
  name: string;
  icon: JSX.Element;
  link: string;
};

export type Experiences = {
  title: string;
  type: string;
  company: string;
  description: string;
  date: string;
};
